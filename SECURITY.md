# Security

Do not post licence keys, proxy credentials, subscription URLs, passwords, MAC
addresses, IMEI/serial values, activation responses, or diagnostic logs in a
public issue. Report sensitive problems privately to the repository owner.

Verify every archive against the SHA-256 printed in both the release notes and
this repository before extraction. Windows will show **Unknown publisher** for
the current experimental build.
